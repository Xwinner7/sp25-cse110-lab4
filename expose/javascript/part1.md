1. What was being printed by line 9 is `values added: 20`.
2.  What was being printed by line 13 is `final result: 20`.
3.  Should not use `var` since it is function-scoped rather than block-scoped. Meaning that a variable created inside an `if` or `for` statement may still exist outside of that statement, which might result in unexpected behavior. Also means hoisted that if read it by mistake before it is allocated, might simply get undefined rather than a clear error. Lastly, we can redeclare a variable without notice, which may cause small problems and overwrite values.
4.  What was being printed by line 9 is `values added: 20`.
5.  What was being printed by line 13 is `console.log('final result: ', result); ReferenceError: result is not defined`. Line 13 would now throws the error `ReferenceError: result is not defined` instead of printing. This is because the second `console.log` is outside of the `if` statement and since the `let` variable are block-scoped, the `result` defined outside of `if` would not exist as `let` is defined inside the `if` statement.
6. Nothing is being printed by line 9 with the statement `result = num1 + num2;`, it throws the error `TypeError: Assignment to constant variable.` This is because we declared `result` with `const` variable, so we would not be able to change its value from 0 to 20, thus `console.log` was never reached and runs.
7. Nothing will be printed by line 13, this is because the function already throws an error at line 7 when running through the `result`, thus it never got a chance to reach and run the second `console.log`.